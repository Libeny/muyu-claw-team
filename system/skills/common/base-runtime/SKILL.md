# Base Runtime Skill

Use the workspace files in the current session as the source of truth.

Rules:
- Read `CLAUDE.md` before acting.
- Prefer the mounted session workspace over absolute host paths.
- Store durable user information in linked user memory files when explicitly asked.
